/target
/internals

# The web bundle. Built with `trunk build --release`, not committed.
/dist

# Secrets: never commit. The example file without a value is the only exception.
.env
.env.*
!.env.example

# JetBrains
/.idea

# Finder leaves these in every directory it is asked to look at.
.DS_Store

# What wrangler remembers about the account it deployed the Worker to.
.wrangler/

CLAUDE.md
